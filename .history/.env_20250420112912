# Replace with your actual Supabase/PostgreSQL connection details
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your_secure_password # Replace with a strong password
DB_HOST=db # This should match the service name in docker-compose.yml
DB_PORT=5432

# Django settings
SECRET_KEY='django-insecure-=your-very-secret-key-here-=#' # Replace with a real secret key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0 # Ensure 0.0.0.0 is here
